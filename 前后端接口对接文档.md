# VisionInfer 后端接口对接文档

## 1. 基本说明

- 项目名称：`VisionInfer`
- 默认服务地址：`http://127.0.0.1:9527`
- 局域网联调示例：`http://192.168.88.131:9527`
- 数据格式：
  - 普通业务接口默认返回 `application/json`
  - 视频结果接口返回 `video/mp4`
- 当前未使用登录态 Token 机制，用户模块接口按显式参数传递

统一返回格式大多数为：

```json
{
  "code": 200,
  "msg": "ok",
  "data": {}
}
```

常见状态码：

- `200`：成功
- `400`：请求参数错误
- `401`：登录失败
- `404`：资源不存在
- `500`：服务端处理失败

## 2. 推荐联调顺序

前端建议按下面顺序联调：

1. 用户登录或获取用户名
2. 上传视频 `POST /api/video/upload`
3. 提交任务 `POST /api/task/submit`
4. 查询任务状态 `GET /api/task/status`
5. 播放结果视频 `GET /api/video/result`
6. 获取任务列表/统计/系统概览

## 3. 用户模块

### 3.1 添加用户

- 方法：`POST`
- 路径：`/api/user/add`
- 请求头：`Content-Type: application/json`

请求体示例：

```json
{
  "username": "chenwanyao",
  "password": "123456",
  "nickname": "陈晚遥",
  "employee_id": "001",
  "email": "test@example.com",
  "phone": "13800138000",
  "department": "Vision",
  "location": "Shanghai",
  "timezone": "Asia/Shanghai",
  "bio": "test user",
  "role": "student"
}
```

成功响应示例：

```json
{
  "code": 200,
  "msg": "添加用户成功"
}
```

### 3.2 用户登录

- 方法：`POST`
- 路径：`/api/user/login`

请求体示例：

```json
{
  "username": "chenwanyao",
  "password": "123456"
}
```

成功响应示例：

```json
{
  "code": 200,
  "msg": "登录成功",
  "data": {
    "id": 1,
    "username": "chenwanyao",
    "role": "student"
  }
}
```

### 3.3 获取用户信息

- 方法：`GET`
- 路径：`/api/user/info`
- Query：`username`

示例：

```text
GET /api/user/info?username=chenwanyao
```

### 3.4 修改用户信息

- 方法：`POST`
- 路径：`/api/user/update`

请求体示例：

```json
{
  "username": "chenwanyao",
  "nickname": "陈晚遥",
  "employee_id": "001",
  "email": "new@example.com",
  "phone": "13800138000",
  "department": "Vision",
  "location": "Shanghai",
  "timezone": "Asia/Shanghai",
  "bio": "updated bio",
  "role": "student"
}
```

## 4. 视频上传与任务模块

### 4.1 上传视频

- 方法：`POST`
- 路径：`/api/video/upload`
- 推荐方式：`multipart/form-data`

表单字段：

- `submitted_by`：文本，必填
- `file`：文件，必填

前端 `FormData` 示例：

```javascript
const formData = new FormData();
formData.append("submitted_by", "chenwanyao");
formData.append("file", fileInput.files[0]);
```

成功响应示例：

```json
{
  "code": 200,
  "msg": "视频上传成功",
  "data": {
    "submitted_by": "chenwanyao",
    "original_filename": "demo.mp4",
    "stored_filename": "1778555394896_demo4.mp4",
    "stored_path": "/home/chenwanyao/graduation_project/storage/videos/input/1778555394896_demo4.mp4",
    "file_size_bytes": 9554424,
    "upload_mode": "multipart_form_data"
  }
}
```

前端后续提交任务时，请使用返回的 `stored_path` 作为 `input_video_path`。

### 4.2 提交分析任务

- 方法：`POST`
- 路径：`/api/task/submit`
- 请求头：`Content-Type: application/json`

请求体示例：

```json
{
  "task_name": "宿舍楼夜间刀具巡检",
  "task_type": "knife_detection",
  "submitted_by": "chenwanyao",
  "input_video_path": "/home/chenwanyao/graduation_project/storage/videos/input/1778555394896_demo4.mp4",
  "frame_interval": 10,
  "confidence_threshold": 0.6,
  "model_id": 1
}
```

字段说明：

- `task_name`：任务名称，可选，不传则后端自动生成
- `task_type`：必填，当前支持：
  - `violation_detection`
  - `vehicle_detection`
  - `knife_detection`
- `submitted_by`：必填，建议传当前登录用户名
- `input_video_path`：必填，使用上传接口返回的 `stored_path`
- `frame_interval`：必填或使用默认值 `1`，值越大处理越快
- `confidence_threshold`：范围 `(0, 1]`
- `model_id`：可选，不传时优先使用当前激活模型

成功响应示例：

```json
{
  "code": 200,
  "msg": "任务提交成功",
  "data": {
    "task_id": 12,
    "status": "PENDING",
    "task_name": "宿舍楼夜间刀具巡检",
    "task_type": "knife_detection",
    "model_id": 1
  }
}
```

### 4.3 查询单个任务状态

- 方法：`GET`
- 路径：`/api/task/status`
- Query：`id`

示例：

```text
GET /api/task/status?id=12
```

返回重点字段：

- 基础信息：
  - `id`
  - `task_name`
  - `task_type`
  - `submitted_by`
  - `status`
- 输入输出：
  - `input_video_path`
  - `output_video_path`
  - `result_url`
- 视频元信息：
  - `video_duration`
  - `video_width`
  - `video_height`
  - `video_fps`
- 处理结果结构化字段：
  - `processed_frame_count`
  - `detection_count`
  - `real_inference_executed`
  - `result_video_generated`
  - `used_model_name`
  - `used_model_framework`
  - `video_build_mode`
  - `inference_runtime_message`
- 展示辅助字段：
  - `has_result_video`
  - `detection_summary_ready`
- 其他：
  - `result_summary`
  - `error_message`
  - `created_at`
  - `started_at`
  - `finished_at`

成功响应示例：

```json
{
  "code": 200,
  "msg": "查询任务成功",
  "data": {
    "id": 12,
    "task_name": "宿舍楼夜间刀具巡检",
    "task_type": "knife_detection",
    "submitted_by": "chenwanyao",
    "status": "COMPLETED",
    "input_video_path": "/home/chenwanyao/graduation_project/storage/videos/input/1778555394896_demo4.mp4",
    "output_video_path": "/home/chenwanyao/graduation_project/storage/videos/output/1778555394896_demo4_task_12_result.mp4",
    "result_url": "/api/video/result?task_id=12",
    "video_duration": 38.2,
    "video_width": 1920,
    "video_height": 1080,
    "video_fps": 25.0,
    "frame_interval": 10,
    "confidence_threshold": 0.6,
    "processed_frame_count": 215,
    "detection_count": 37,
    "real_inference_executed": true,
    "result_video_generated": true,
    "used_model_name": "yolov8n_onnx",
    "used_model_framework": "onnx",
    "video_build_mode": "ffmpeg_reencode",
    "inference_runtime_message": "真实前向推理成功 ...",
    "has_result_video": true,
    "detection_summary_ready": true,
    "result_summary": "......",
    "error_message": "",
    "model_id": 1,
    "created_at": "2026-05-12 20:10:00",
    "started_at": "2026-05-12 20:10:01",
    "finished_at": "2026-05-12 20:12:05"
  }
}
```

### 4.4 查询任务列表

- 方法：`GET`
- 路径：`/api/task/list`

支持 Query：

- `limit`：返回条数，范围 `1-100`
- `status`：按任务状态筛选
- `task_type`：按任务类型筛选
- `submitted_by`：按提交人筛选

示例：

```text
GET /api/task/list?limit=20
GET /api/task/list?status=COMPLETED
GET /api/task/list?task_type=knife_detection
GET /api/task/list?status=FAILED&submitted_by=chenwanyao&limit=10
```

响应结构：

```json
{
  "code": 200,
  "msg": "查询任务列表成功",
  "data": {
    "total": 2,
    "limit": 10,
    "filters": {
      "status": "COMPLETED",
      "task_type": "knife_detection",
      "submitted_by": "chenwanyao"
    },
    "items": [
      {
        "id": 12,
        "task_name": "宿舍楼夜间刀具巡检",
        "status": "COMPLETED"
      }
    ]
  }
}
```

`items` 中每个元素字段与 `GET /api/task/status` 的 `data` 基本一致。

### 4.5 查询任务统计

- 方法：`GET`
- 路径：`/api/task/stats`

响应示例：

```json
{
  "code": 200,
  "msg": "查询任务统计成功",
  "data": {
    "total": 12,
    "result_video_generated": 8,
    "real_inference_executed": 7,
    "by_status": {
      "PENDING": 1,
      "PROCESSING": 2,
      "COMPLETED": 8,
      "FAILED": 1
    },
    "by_task_type": {
      "knife_detection": 6,
      "vehicle_detection": 4,
      "violation_detection": 2
    }
  }
}
```

### 4.6 获取结果视频

- 方法：`GET`
- 路径：`/api/video/result`
- Query：`task_id`
- 返回类型：`video/mp4`

示例：

```text
GET /api/video/result?task_id=12
```

前端可直接用：

```html
<video controls src="http://127.0.0.1:9527/api/video/result?task_id=12"></video>
```

## 5. 模型管理模块

### 5.1 新增模型

- 方法：`POST`
- 路径：`/api/model/add`

请求体示例：

```json
{
  "model_name": "yolov8n_onnx",
  "file_path": "/home/chenwanyao/graduation_project/storage/models/yolov8n.onnx",
  "framework": "onnx",
  "uploaded_by": "chenwanyao",
  "is_active": true
}
```

### 5.2 切换当前模型

- 方法：`POST`
- 路径：`/api/model/switch`

请求体示例：

```json
{
  "model_id": 1
}
```

### 5.3 查询当前激活模型

- 方法：`GET`
- 路径：`/api/model/current`

### 5.4 查询模型列表

- 方法：`GET`
- 路径：`/api/model/list`

返回字段：

- `id`
- `model_name`
- `file_path`
- `framework`
- `is_active`
- `uploaded_by`
- `uploaded_at`
- `updated_at`

## 6. 系统监控与概览模块

### 6.1 获取系统状态

- 方法：`GET`
- 路径：`/api/system/status`

用途：

- 更偏运行时状态与稳定性监控
- 适合展示“当前负载”和“并发承载情况”

主要字段：

- 当前值：
  - `current_connections`
  - `pending_tasks`
  - `active_threads`
  - `completed_tasks`
  - `failed_tasks`
  - `total_requests`
- 峰值与性能：
  - `peak_connections`
  - `peak_pending_tasks`
  - `peak_active_threads`
  - `uptime_seconds`
  - `request_throughput_per_second`
  - `avg_task_duration_ms`
  - `max_task_duration_ms`
  - `task_success_rate`

同时它还会聚合：

- `runtime`
- `tasks`
- `models`

### 6.2 获取系统概览

- 方法：`GET`
- 路径：`/api/system/overview`

用途：

- 更偏首页总览/仪表盘
- 一次拿全系统、任务、模型三类摘要

返回结构：

```json
{
  "code": 200,
  "msg": "获取系统概览成功",
  "data": {
    "runtime": {},
    "tasks": {},
    "models": {}
  }
}
```

`runtime`、`tasks`、`models` 的含义：

- `runtime`
  - 系统当前连接、队列、线程、吞吐、平均任务耗时、成功率等
- `tasks`
  - 总任务数、按状态统计、按任务类型统计、真实推理执行数、结果视频生成数
- `models`
  - 模型总数、激活模型数、按框架统计、当前激活模型信息

## 7. 前端页面建议字段映射

### 7.1 任务列表页建议直接使用

- `task_name`
- `task_type`
- `submitted_by`
- `status`
- `created_at`
- `processed_frame_count`
- `detection_count`
- `used_model_name`
- `result_video_generated`

### 7.2 任务详情页建议直接使用

- `video_duration`
- `video_width`
- `video_height`
- `video_fps`
- `frame_interval`
- `confidence_threshold`
- `real_inference_executed`
- `video_build_mode`
- `inference_runtime_message`
- `result_url`
- `error_message`
- `result_summary`

### 7.3 首页/仪表盘建议直接使用

- `runtime.current_connections`
- `runtime.peak_connections`
- `runtime.pending_tasks`
- `runtime.request_throughput_per_second`
- `runtime.avg_task_duration_ms`
- `runtime.task_success_rate`
- `tasks.by_status`
- `tasks.by_task_type`
- `models.current_active_model`

## 8. 推荐联调流程示例

### 8.1 视频分析完整流程

1. 上传视频

```text
POST /api/video/upload
```

2. 提交任务

```text
POST /api/task/submit
```

3. 轮询任务状态

```text
GET /api/task/status?id=任务ID
```

4. 播放结果视频

```text
GET /api/video/result?task_id=任务ID
```

### 8.2 管理后台首页流程

1. 获取系统概览

```text
GET /api/system/overview
```

2. 获取最近任务

```text
GET /api/task/list?limit=10
```

3. 获取当前模型

```text
GET /api/model/current
```

## 9. 联调注意事项

- `POST /api/video/upload` 推荐使用 `multipart/form-data`
- `POST /api/task/submit` 使用 JSON，不直接上传文件
- `input_video_path` 使用上传接口返回的 `stored_path`
- 结果视频接口返回的是二进制流，不是 JSON
- 如果任务处理时间较长，前端应采用轮询方式查询任务状态
- `result_summary` 主要给人看；前端优先使用结构化字段，不要依赖解析中文摘要

